/* Estou a criar o contentor da tabela com scroll horizontal para que em ecrãs pequenos se possa deslizar */
.tabela-contentor {
    /* Estou a permitir que a tabela deslize horizontalmente em telemóveis sem quebrar o layout da página */
    overflow-x: auto;
    /* Estou a dar cantos arredondados ao contentor da tabela para manter o aspecto suave do design system */
    border-radius: var(--raio-borda);
}

/* Estou a definir o estilo base de todas as tabelas da plataforma ARMS para terem uma aparência uniforme */
.tabela-arms {
    /* Estou a esticar a tabela para ocupar toda a largura do contentor pai */
    width: 100%;
    /* Estou a alinhar o texto das células à esquerda para facilitar a leitura de dados textuais */
    text-align: left;
    /* Estou a colapsar as bordas para que as linhas de separação fiquem finas e limpas */
    border-collapse: collapse;
}

/* Estou a estilizar as células do cabeçalho da tabela (os títulos das colunas) */
.tabela-arms thead th {
    /* Estou a dar espaço interno confortável para os títulos respirarem e serem fáceis de ler */
    padding: 14px 16px;
    /* Estou a usar a cor de texto secundário para que os títulos não gritem mais alto que os dados */
    color: var(--texto-secundario);
    /* Estou a dar um peso de letra médio para os títulos se destacarem dos dados sem serem muito pesados */
    font-weight: 500;
    /* Estou a reduzir ligeiramente o tamanho dos títulos para criarem uma hierarquia visual com os dados */
    font-size: 0.875rem;
    /* Estou a adicionar uma linha fina no fundo do cabeçalho para separar dos dados */
    border-bottom: 2px solid #e4e4e7;
    /* Estou a impedir que os títulos das colunas quebrem em várias linhas */
    white-space: nowrap;
}

/* Estou a estilizar cada célula de dados no corpo da tabela */
.tabela-arms tbody td {
    /* Estou a dar o mesmo espaço interno que os títulos para ficarem alinhados na perfeição */
    padding: 16px;
    /* Estou a adicionar uma linha muito fina e clara entre cada linha de dados */
    border-bottom: 1px solid #f4f4f5;
    /* Estou a usar a cor de texto principal para os dados serem perfeitamente legíveis */
    color: var(--texto-principal);
}

/* Estou a criar um efeito sutil quando o utilizador passa o rato por cima de uma linha da tabela */
.tabela-arms tbody tr {
    /* Estou a preparar a transição suave para o efeito de hover na linha */
    transition: background-color 0.2s ease;
}

/* Estou a destacar a linha da tabela quando o rato passa por cima para facilitar a leitura de dados */
.tabela-arms tbody tr:hover {
    /* Estou a dar um tom cinza muito leve à linha inteira para o utilizador saber onde está a olhar */
    background-color: #fafafa;
}

/* Estou a criar a barra de ferramentas que fica acima da tabela com filtros e botões de acção */
.tabela-barra {
    /* Estou a alinhar os filtros e botões lado a lado com espaço entre eles */
    display: flex;
    justify-content: space-between;
    align-items: center;
    /* Estou a dar um espaço abaixo da barra antes da tabela começar */
    margin-bottom: 20px;
    /* Estou a permitir que em ecrãs pequenos os elementos se rearranjem em várias linhas */
    flex-wrap: wrap;
    gap: 12px;
}

/* Estou a estilizar o contador de resultados que aparece abaixo da tabela (Ex: "A mostrar 5 de 10 pedidos") */
.tabela-rodape {
    /* Estou a alinhar o contador e os botões de paginação em extremos opostos */
    display: flex;
    justify-content: space-between;
    align-items: center;
    /* Estou a dar espaço acima do rodapé para separar da última linha da tabela */
    margin-top: 16px;
    padding-top: 16px;
    border-top: 1px solid #f4f4f5;
}

/* Estou a estilizar o texto do contador de resultados para ser subtil e informativo */
.tabela-rodape .contador-resultados {
    /* Estou a usar o texto secundário e um tamanho mais pequeno para não distrair */
    color: var(--texto-secundario);
    font-size: 0.875rem;
}
