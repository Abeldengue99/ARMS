<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="600" height="200" viewBox="0 0 600 200" role="img" aria-label="ARMS - Aksanti Request Management System">
  <defs>
    <linearGradient id="g1" x1="0%" x2="100%" y1="0%" y2="0%">
      <stop offset="0%" stop-color="#f59e0b"/>
      <stop offset="100%" stop-color="#d97706"/>
    </linearGradient>
    <style>
      .title{font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial; font-weight:800; fill:url(#g1); font-size:88px; letter-spacing:2px}
      .tag{font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial; font-weight:600; fill:#334155; font-size:18px}
    </style>
  </defs>

  <!-- Ornament removed as requested -->

  <!-- Main text -->
  <text x="50%" y="78" class="title" text-anchor="middle">ARMS</text>

  <!-- Tagline (restored) -->
  <text x="50%" y="136" class="tag" text-anchor="middle">Aksanti Request Management System</text>
</svg>